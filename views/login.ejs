<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login-Account</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900">
  <div class="flex items-center justify-center min-h-screen">
    <form class="bg-zinc-800 p-8 rounded-lg shadow-lg w-full max-w-md space-y-6" action="/login" method="post">
      <h2 class="text-3xl font-bold text-center text-gray-100 mb-6">Login to Your Account</h2>
      <div>

        <input 
          type="email" 
          name="email" 
          placeholder="Enter your email" 
          class="w-full px-4 py-2 border border-gray-600 bg-zinc-900 text-gray-200 rounded-md focus:ring-2 focus:ring-blue-500 focus:outline-none"
        >
      </div>
      <div>

        <input 
          type="password" 
          name="password" 
          placeholder="Enter your password" 
          class="w-full px-4 py-2 border border-gray-600 bg-zinc-900 text-gray-200 rounded-md focus:ring-2 focus:ring-blue-500 focus:outline-none"
        >
      </div>
      <div>
        <input 
          type="submit" 
          value="Login" 
          class="w-full bg-blue-500 text-white py-2 rounded-md hover:bg-blue-600 transition duration-300 ease-in-out cursor-pointer"
        >
      </div>
      <!-- Forgot Password and Register Link -->
      <div class="text-center text-gray-400 mt-4">
        <p class="mt-2">Don't have an account? 
          <a href="/" class="text-blue-500 hover:underline">Sign up</a>
        </p>
      </div>
    </form>
  </div>
</body>
</html>
