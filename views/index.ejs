<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create-User</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900">
  <div class="flex items-center justify-center min-h-screen">
    <form class="bg-zinc-800 p-6 rounded-lg shadow-md w-full max-w-md space-y-4"  action="/register" method="post">
      <h2 class="text-2xl font-bold text-center text-gray-100 mb-4">Create Account</h2>
      <div>
        <input 
          type="text" 
          name="name" 
          placeholder="Name" 
          class="w-full px-4 py-2 border border-gray-600 bg-zinc-900 text-gray-200 rounded-md focus:ring-2 focus:ring-blue-500 focus:outline-none"
        >
      </div>
      <div>
        <input 
          type="text" 
          name="username" 
          placeholder="Username" 
          class="w-full px-4 py-2 border border-gray-600 bg-zinc-900 text-gray-200 rounded-md focus:ring-2 focus:ring-blue-500 focus:outline-none"
        >
      </div>
      <div>
        <input 
          type="number" 
          name="age" 
          placeholder="Age" 
          class="w-full px-4 py-2 border border-gray-600 bg-zinc-900 text-gray-200 rounded-md focus:ring-2 focus:ring-blue-500 focus:outline-none"
        >
      </div>
      <div>
        <input 
          type="email" 
          name="email" 
          placeholder="Email" 
          class="w-full px-4 py-2 border border-gray-600 bg-zinc-900 text-gray-200 rounded-md focus:ring-2 focus:ring-blue-500 focus:outline-none"
        >
      </div>
      <div>
        <input 
          type="password" 
          name="password" 
          placeholder="Password" 
          class="w-full px-4 py-2 border border-gray-600 bg-zinc-900 text-gray-200 rounded-md focus:ring-2 focus:ring-blue-500 focus:outline-none"
        >
      </div>
      <div>
        <input 
          type="submit" 
          value="Create Account" 
          class="w-full bg-blue-500 text-white py-2 rounded-md hover:bg-blue-600 cursor-pointer"
        >
      </div>
            <!-- Add "Already have an account?" -->
            <div class="text-center text-gray-400">
              <p>Already have an account? 
                <a href="/login" class="text-blue-500 hover:underline">Log in</a>
              </p>
            </div>
    </form>
  </div>
</body>
</html>
